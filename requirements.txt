# 保险知识问答生成管道 - 依赖

# ---------- 核心依赖 ----------
pyyaml>=6.0
pdfplumber>=0.10.0
pypdf>=4.0
python-docx>=1.0

# ---------- LLM SDK ----------
anthropic>=0.40.0
openai>=1.50.0          # 同时用于 DashScope 兼容模式

# ---------- 质检 ----------
# 可选：embedding 去重（如不安装则自动退化为 n-gram 去重）
# sentence-transformers>=3.0.0
# numpy>=1.24

# ---------- Milvus（如需向量数据库去重） ----------
# pymilvus>=2.4.0

# ---------- 环境变量 ----------
python-dotenv>=1.0.0

# ---------- Web 前端服务 ----------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
sse-starlette>=2.1.0
